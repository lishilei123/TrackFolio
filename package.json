{
  "name": "trackfolio",
  "version": "0.1.27",
  "private": true,
  "description": "股票与基金盈亏盯盘分析网站",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "app/server",
    "app/web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace @trackfolio/web && npm run build --workspace @trackfolio/server",
    "start": "npm run start --workspace @trackfolio/server",
    "test": "npm run test --workspace @trackfolio/server"
  }
}
