services:
  server:
    build:
      context: .
      target: server
    environment:
      NODE_ENV: production
      PORT: "5174"
      LOG_LEVEL: ${LOG_LEVEL:-info}
      TRACKFOLIO_DB: /data/trackfolio.sqlite
      TRACKFOLIO_PROVIDER: ${TRACKFOLIO_PROVIDER:-auto}
    volumes:
      - trackfolio-data:/data
    expose:
      - "5174"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:5174/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped

  web:
    build:
      context: .
      target: web
    depends_on:
      server:
        condition: service_healthy
    ports:
      - "${TRACKFOLIO_HTTP_PORT:-8080}:80"
    restart: unless-stopped

volumes:
  trackfolio-data:
